内容标题为：{{ title }}，一级标签为{{ category }}。类型为{{ item_type }}，发布于{{ pub_time | int }}，{{ '已' if status else '未' }}被推荐。
作者身份为{{ author_status }}，发布源为{{ publish_source }}。
内容获得{{ praise_count | int }}点赞，{{ comment_count | int }}评论，{{ collect_count | int }}收藏，{{ share_count | int }}分享。
根据以上内容特征生成综合表征向量：
